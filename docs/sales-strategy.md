# 판매 전략 및 비즈니스 모델

> 작성일: 2026-02-04
>
> Cuggu 청첩장 플랫폼의 판매 전략, 회원 시스템, 결제 방식 비교

## 목차

1. [현재 구조의 문제점](#현재-구조의-문제점)
2. [판매 전략 비교](#판매-전략-비교)
3. [회원가입 필요성](#회원가입-필요성)
4. [경로 재구성](#경로-재구성)
5. [스마트스토어 구현 방식](#스마트스토어-구현-방식)
6. [추천 전략](#추천-전략)

---

## 현재 구조의 문제점

### 경로명 혼란

현재 `/admin` 경로가 **일반 사용자의 개인 대시보드**로 사용되고 있어 혼란을 줌.

**문제:**
- "admin"은 관리자 전용 페이지를 연상시킴
- 실제로는 로그인한 모든 사용자가 사용하는 작업 공간
- 일반 사용자가 접근하기 꺼려질 수 있음

**해결:**
- `/admin` → `/dashboard` 또는 `/my`로 변경
- 실제 관리자 페이지는 별도 경로 (`/admin` with role check)

---

## 판매 전략 비교

### 1. 자체 결제 (Toss Payments) ⭐ 추천

#### 사용자 흐름

```
회원가입 (카카오/네이버)
  ↓
청첩장 생성 (무료 템플릿)
  ↓
프리미엄 원하면 → 사이트 내 결제 (9,900원)
  ↓
즉시 활성화 (DB 업데이트)
```

#### 장점

- ✅ **통합된 UX**: 사이트 안에서 완결
- ✅ **낮은 수수료**: 2.9% (Toss Payments)
- ✅ **데이터 통합**: 결제-사용자-청첩장 완전 연결
- ✅ **즉시 활성화**: 결제 후 바로 프리미엄 기능 사용
- ✅ **충동 구매 유도**: 편집 중 바로 업그레이드 가능

#### 단점

- ❌ PG 연동 개발 필요 (2-3일 소요)
- ❌ 초기 신뢰도 낮음 (무명 사이트)
- ❌ 사업자등록증 필요

#### 구현 복잡도

**Phase 1 (MVP):**
```typescript
// 1. Toss Payments 결제 요청
POST /api/payment/toss/request
{
  amount: 9900,
  orderId: "order_xxx",
  orderName: "Cuggu 프리미엄 업그레이드"
}

// 2. 결제 승인
POST /api/payment/toss/confirm
{
  paymentKey: "xxx",
  orderId: "order_xxx",
  amount: 9900
}

// 3. DB 업데이트
UPDATE users SET premium_plan = 'PREMIUM' WHERE id = 'user_xxx'
```

---

### 2. 네이버 스마트스토어

#### 사용자 흐름

```
스마트스토어에서 "Cuggu 프리미엄" 구매
  ↓
쿠폰 코드 받음 (예: CUGGU-ABC123)
  ↓
Cuggu 사이트 가입 + 쿠폰 입력
  ↓
프리미엄 활성화
```

#### 장점

- ✅ **네이버 신뢰도**: 스마트스토어 브랜드 활용
- ✅ **트래픽 유입**: 네이버 검색/쇼핑 노출
- ✅ **결제 인프라 불필요**: 네이버가 처리
- ✅ **사업자등록 없어도 시작 가능** (개인 판매자)

#### 단점

- ❌ **수수료 높음**: 3.5-5% (카테고리별 차이)
- ❌ **UX 단절**: 구매 → 사이트 이동 → 코드 입력
- ❌ **충동 구매 어려움**: 편집 중 바로 업그레이드 불가
- ❌ **데이터 분리**: 스마트스토어 주문 DB와 Cuggu DB 별도
- ❌ **쿠폰 관리 부담**: 코드 생성, 발송, 검증

#### 구현 복잡도

```sql
-- 쿠폰 테이블 추가
CREATE TABLE coupons (
  code VARCHAR(20) PRIMARY KEY,
  type ENUM('PREMIUM', 'AI_CREDITS_10'),
  is_used BOOLEAN DEFAULT FALSE,
  used_by VARCHAR(128) REFERENCES users(id),
  used_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);
```

```typescript
// 쿠폰 입력 API
POST /api/coupon/redeem
{
  code: "CUGGU-ABC123"
}

// 검증 및 활성화
1. 쿠폰 존재 확인
2. 사용 여부 확인
3. 사용자 프리미엄 활성화
4. 쿠폰 사용 처리
```

---

### 3. 하이브리드 전략 ⭐⭐ 최종 추천

#### Phase 1: MVP (첫 3개월)

- **주력**: 자체 결제 (Toss Payments)
- **옵션**: 스마트스토어 (신뢰도 확보용)

#### Phase 2: 출시 후 (3-6개월)

- **주력**: 자체 결제 (할인 쿠폰으로 유도)
- **서브**: 스마트스토어 (네이버 유입 채널)

#### Phase 3: 안정화 (6개월 이후)

- **주력**: 자체 결제 (90%)
- **서브**: 스마트스토어 (10%, 네이버 노출 유지)

#### 전환 유도 전략

| 시점 | 자체 결제 | 스마트스토어 |
|------|-----------|--------------|
| MVP | 9,900원 | 11,000원 |
| 3개월 후 | 9,900원 (첫 구매 10% 할인) | 11,000원 |
| 6개월 후 | 9,900원 (상시 쿠폰) | 11,900원 |

---

## 회원가입 필요성

### 무조건 필요함 ✅

#### 이유

1. **청첩장 소유권**
   - 누가 만들었는지 식별
   - 무단 편집 방지

2. **편집 권한**
   - 본인만 수정 가능
   - 협업 시 권한 관리

3. **결제 연결**
   - 누가 결제했는지 추적
   - 환불 처리 가능

4. **RSVP 통계**
   - 하객 응답을 주인에게 전달
   - 실시간 통계 제공

5. **재방문**
   - 나중에 다시 와서 수정
   - 여러 청첩장 관리 (재혼, 돌잔치 등)

### 간편하게 만들기

#### 원칙

- ❌ 이메일/비밀번호 입력
- ✅ 소셜 로그인만 (카카오/네이버)
- ✅ 1-click 가입
- ✅ 필수 정보 최소화

#### UI/UX

```
┌─────────────────────────────────────┐
│                                     │
│   [🟡 카카오로 3초만에 시작하기]   │
│                                     │
│   [🟢 네이버로 시작하기]            │
│                                     │
│   가입과 동시에 이용약관 및         │
│   개인정보처리방침에 동의합니다.    │
│                                     │
└─────────────────────────────────────┘
```

#### 가입 흐름

```
카카오 버튼 클릭
  ↓
카카오 로그인 (카카오 페이지)
  ↓
정보 제공 동의 (카카오 페이지)
  ↓
Cuggu로 리다이렉트
  ↓
자동 회원가입 + 로그인 완료
  ↓
대시보드 이동 (/dashboard)
```

---

## 경로 재구성

### 현재 (문제)

```
/admin              → 일반 사용자 대시보드 (이름 혼란)
/admin/invitations  → 내 청첩장
/admin/ai-photos    → AI 사진 생성
/admin/settings     → 설정
```

### 변경 후 (권장)

```
/                          → 랜딩 페이지
/login                     → 로그인 (소셜 로그인만)
/pricing                   → 가격표

# 로그인 필요
/dashboard                 → 내 대시보드 (기존 /admin)
/dashboard/invitations     → 내 청첩장 목록
/dashboard/ai-photos       → AI 사진 생성
/dashboard/settings        → 설정
/editor/[id]               → 청첩장 편집기

# 비로그인 접근 가능
/inv/[id]                  → 청첩장 공개 페이지
/rsvp/[id]                 → RSVP 응답

# 관리자 전용 (role=ADMIN)
/admin                     → 관리자 대시보드
/admin/users               → 사용자 관리
/admin/payments            → 결제 내역
/admin/analytics           → 통계
```

### 리다이렉트 처리

```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  const { pathname } = request.nextUrl;

  // /admin/* → /dashboard/* 리다이렉트 (하위 호환)
  if (pathname.startsWith('/admin') && !isAdmin(request)) {
    return NextResponse.redirect(
      new URL(pathname.replace('/admin', '/dashboard'), request.url)
    );
  }

  // 로그인 필요한 페이지
  if (pathname.startsWith('/dashboard') || pathname.startsWith('/editor')) {
    if (!isAuthenticated(request)) {
      return NextResponse.redirect(new URL('/login', request.url));
    }
  }
}
```

---

## 스마트스토어 구현 방식

### A. 쿠폰 코드 방식 (간단) ⭐

#### 흐름

1. **스마트스토어 상품 등록**
   - 상품명: "Cuggu 프리미엄 이용권"
   - 가격: 11,000원 (자체 결제보다 비쌈)
   - 카테고리: 디지털/콘텐츠

2. **구매 시 쿠폰 발송**
   - 구매 확인 시 쿠폰 코드 생성
   - 카카오톡/문자로 발송
   - 사용법 안내

3. **DB 구조**

```sql
CREATE TABLE coupons (
  code VARCHAR(20) PRIMARY KEY,           -- CUGGU-ABC123
  type ENUM('PREMIUM', 'AI_CREDITS_10'),  -- 쿠폰 타입
  is_used BOOLEAN DEFAULT FALSE,          -- 사용 여부
  used_by VARCHAR(128) REFERENCES users(id), -- 사용자
  used_at TIMESTAMP,                      -- 사용 시각
  expires_at TIMESTAMP,                   -- 만료일 (6개월)
  created_at TIMESTAMP DEFAULT NOW()
);
```

4. **쿠폰 입력 페이지**

```
/dashboard/coupon

┌─────────────────────────────────────┐
│  쿠폰 코드 입력                     │
│                                     │
│  [CUGGU-ABC123        ] [확인]      │
│                                     │
│  스마트스토어에서 구매하셨나요?     │
│  [구매하러 가기]                    │
└─────────────────────────────────────┘
```

5. **검증 API**

```typescript
// POST /api/coupon/redeem
export async function POST(req: Request) {
  const { code } = await req.json();
  const session = await auth();

  // 1. 쿠폰 존재 확인
  const coupon = await db.query.coupons.findFirst({
    where: eq(coupons.code, code)
  });

  if (!coupon) {
    return Response.json({ error: '유효하지 않은 쿠폰입니다' }, { status: 400 });
  }

  // 2. 사용 여부 확인
  if (coupon.is_used) {
    return Response.json({ error: '이미 사용된 쿠폰입니다' }, { status: 400 });
  }

  // 3. 만료 확인
  if (new Date() > coupon.expires_at) {
    return Response.json({ error: '만료된 쿠폰입니다' }, { status: 400 });
  }

  // 4. 트랜잭션
  await db.transaction(async (tx) => {
    // 쿠폰 사용 처리
    await tx.update(coupons)
      .set({
        is_used: true,
        used_by: session.user.id,
        used_at: new Date()
      })
      .where(eq(coupons.code, code));

    // 사용자 프리미엄 활성화
    if (coupon.type === 'PREMIUM') {
      await tx.update(users)
        .set({ premium_plan: 'PREMIUM' })
        .where(eq(users.id, session.user.id));
    } else if (coupon.type === 'AI_CREDITS_10') {
      await tx.update(users)
        .set({ ai_credits: sql`ai_credits + 10` })
        .where(eq(users.id, session.user.id));
    }
  });

  return Response.json({ success: true });
}
```

#### 쿠폰 생성 스크립트

```typescript
// scripts/generate-coupons.ts
import { nanoid } from 'nanoid';

async function generateCoupons(count: number, type: 'PREMIUM' | 'AI_CREDITS_10') {
  const coupons = [];

  for (let i = 0; i < count; i++) {
    const code = `CUGGU-${nanoid(8)}`.toUpperCase();
    const expiresAt = new Date();
    expiresAt.setMonth(expiresAt.getMonth() + 6); // 6개월 후

    coupons.push({
      code,
      type,
      expires_at: expiresAt
    });
  }

  await db.insert(coupons).values(coupons);

  console.log(`${count}개 쿠폰 생성 완료`);
  console.log(coupons.map(c => c.code).join('\n'));
}

// 100개 프리미엄 쿠폰 생성
generateCoupons(100, 'PREMIUM');
```

---

### B. 자동 연동 (복잡, 추후 고려)

#### 네이버 커머스 API 사용

```typescript
// 1. 주문 webhook 수신
POST /api/smartstore/webhook
{
  orderId: "2024010112345",
  productId: "1234567",
  buyerPhone: "010-1234-5678",
  amount: 11000,
  status: "PAID"
}

// 2. 쿠폰 자동 생성
const coupon = await createCoupon('PREMIUM');

// 3. SMS 발송 (Naver Cloud SMS)
await sendSMS({
  to: "010-1234-5678",
  message: `Cuggu 프리미엄 쿠폰: ${coupon.code}`
});
```

#### 단점

- 네이버 커머스 API 연동 복잡
- SMS 발송 비용 (건당 15원)
- 오류 처리 복잡 (결제 취소, 환불 등)

---

## 추천 전략

### MVP (첫 3개월) - 자체 결제만

#### 목표
- 빠른 출시
- 통합된 UX 검증
- 결제 플로우 최적화

#### 구현
- ✅ Toss Payments 연동
- ✅ 카카오/네이버 로그인
- ✅ `/dashboard` 경로
- ❌ 스마트스토어 (나중에)

#### 성공 지표
- 전환율 10% 이상
- 결제 이탈률 30% 이하
- 평균 구매 시간 3분 이내

---

### Phase 2 (출시 후 3개월) - 스마트스토어 추가

#### 조건
- 자체 결제 전환율 < 10%
- 신뢰도 문제로 이탈 많음
- 네이버 트래픽 필요

#### 구현
- ✅ 쿠폰 시스템 추가
- ✅ 스마트스토어 상품 등록
- ✅ 쿠폰 입력 페이지

#### 운영
- 자체 결제 9,900원 (주력)
- 스마트스토어 11,000원 (서브)

---

### Phase 3 (6개월 후) - 최적화

#### 전환 유도
- 자체 결제 할인 쿠폰 (첫 구매 10%)
- 스마트스토어 가격 인상 (11,900원)
- "사이트 결제가 더 저렴해요" 문구

#### 목표
- 자체 결제 비율 90%
- 스마트스토어 10% (네이버 유입만)

---

## 의사결정 트리

```
MVP 출시 준비 중?
  ├─ YES → 자체 결제만 구현 (Toss Payments)
  └─ NO → 이미 출시?
        ├─ 전환율 < 10%? → 스마트스토어 추가
        └─ 전환율 >= 10%? → 자체 결제 최적화
```

---

## 다음 액션 아이템

### 1. 경로 변경 (우선순위: 높음)
- [ ] `/admin` → `/dashboard` 경로 변경
- [ ] middleware 인증 체크 추가
- [ ] 하위 호환 리다이렉트 설정

### 2. 자체 결제 구현 (우선순위: 높음)
- [ ] Toss Payments API 키 발급
- [ ] `/api/payment/toss/*` 엔드포인트 구현
- [ ] 결제 페이지 UI
- [ ] 결제 성공/실패 처리

### 3. 쿠폰 시스템 (우선순위: 중간, Phase 2)
- [ ] `coupons` 테이블 추가
- [ ] `/api/coupon/redeem` API
- [ ] 쿠폰 입력 페이지
- [ ] 쿠폰 생성 스크립트

### 4. 스마트스토어 (우선순위: 낮음, Phase 2)
- [ ] 스마트스토어 판매자 가입
- [ ] 상품 등록 (디지털 콘텐츠)
- [ ] 쿠폰 발송 프로세스 정립

---

## 참고 자료

### Toss Payments
- 개발 가이드: https://docs.tosspayments.com
- 수수료: 2.9% (월 500만원 이하), 2.6% (이상)
- 정산: D+3 (영업일 기준)

### 네이버 스마트스토어
- 가이드: https://sell.smartstore.naver.com
- 수수료: 3.5-5% (카테고리별)
- 정산: 주 1회 (화요일)

### 사업자등록
- Toss Payments: 필수
- 스마트스토어: 개인 판매자 가능 (연 매출 4,800만원 이하)
